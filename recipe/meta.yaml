{% set name = "mlforecast" %}
{% set version = "0.0.7" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/mlforecast-{{ version }}.tar.gz
  sha256: b59b1e260252e7c55b5d4f5f88966e1cea4b0014eb300c1be60ec6f7eb49d344

build:
  number: 0
  noarch: python
  entry_points:
    - mlforecast=mlforecast.cli:app
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python >=3.6
  run:
    - numba
    - pandas
    - pydantic
    - python >=3.6
    - pyyaml
    - typer
    - window-ops

test:
  imports:
    - mlforecast
  commands:
    - mlforecast --help

about:
  home: https://github.com/Nixtla/mlforecast
  dev_url: https://github.com/Nixtla/mlforecast
  doc_url: https://nixtla.github.io/mlforecast/
  license: Apache-2.0
  license_file: LICENSE
  license_family: APACHE
  summary: Scalable machine learning based time series forecasting

extra:
  recipe-maintainers:
    - jose-moralez
